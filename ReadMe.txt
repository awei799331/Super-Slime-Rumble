to run the game, go into dist -> summative -> double click on summative.exe
to run the full version with the trailer, do the following
1. install python 3.6.4
2. in the command line type in following commands (if you don't have the modules installed previously)
	pip install pygame
	pip install ez_setup
	pip install moviepy
	pip install requests
3. run summative.py


